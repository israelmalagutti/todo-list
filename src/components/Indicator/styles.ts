import { StyleSheet } from "react-native";

export const indicatorStyles = StyleSheet.create({
  container: {
    backgroundColor: "#333333",
    alignItems: "center",
    justifyContent: "center",
    paddingVertical: 2,
    paddingHorizontal: 8,
    borderRadius: 999,
  },

  text: { color: "#D9D9D9", fontSize: 12, fontWeight: "700" },
});
